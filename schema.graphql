type Query {
  users: [User!]!
}

type Mutation {
  createUser(name: String!, email: String): User
  updateUser(username: String!, newname: String!): User
}

type User {
  id: ID!
  email: String
  name: String!
}
